Knowledge on Git Command-Line (Shared by Bucky Roberts Youtube channel)

Step1: Downloading/Installing git from GitHub

Step2: Configure Username/Email 

Step3: Creating first repository

Step4: Commit

Step5: Adding Files and Commit Log

Step6: Git Workflow

Step7:
Step8:
Step9:
Step10:
Step11:
Step12:
Step13:
Step14:
Step15:
Step16:
Step17:
Step18:
Step19:
Step20:

..................................................................................................................................................................

For instance, the required folder is on Desktop
// Basic gitBash/Terminal commands will be as follows:

$ clear                                                           // Clears the current screen
$ history -c                                                      // Clear command line history                                                           
$ q                                                               // Exit
$ CTRL+C                                                          // Stop
$ stop                                                            // Stop
$ cd                                                              // Path
$ cd ..                                                           // Change Directory
$ cd ~                                                            // Home
$ ls                                                              // Files contained
$ ls -la                                                          // Hidden files can be viewed
$ pwd                                                             // Directory
$ touch sample.txt                                                // Create a text file
$ open sample.txt                                                 // Open sample.txt

//Updating the configuration file
$ git config --global user.name "Vatsal Salla"                    // Account setup
$ git config --global user.name                                   // Press Enter to view the stored user name
$ git config --global user.email "Type the email here"            // Press Enter to view the stored user email
$ git config --list                                               // See all user credentials

// WORKING COPY (Local Computer) >> STAGING AREA (Files that are ready for commited) >> REPOSITORY/PROJECT
// EDIT >> MODIFY >> ADD >> COMMIT

$ git help                                                        // Help
$ git init                                                        // Initialize Repository/Project. Git will now monitor/track changes of files inside that folder
$ git add .                                                       // Add all the files to staging area
$ git add fileName                                                // Add a single file to staging area
$ git commit -m "Comments to remember the point of time"          // This is to store the point of time. Taking a snapshot of that time and storing it.
$ git commit -am "To commit everything"
$ git log                                                         // View commit history
$ git log --author="authorName".                                  // View specific Author's commits
$ git status                                                      // Compare repository and main directory
$ git diff                                                        // View the changes in Bash
$ git reset HEAD fileName                                         // Unstage 

// Renaming Files

$ git rm fileName                                                 // Remove the file from both the places
$ git add renameIt                                                // Renaming in the main repository
$ git rm oldFileName                                              // Remove old file

// Moving Files

$ git oldFileName newFileName                                     // Renaming made easy
$ git checkout --fileName                                         // Check the file
$ git checkout commitNumber --index                               // First >> Second >> Third >> Second

// Creating files

$ touch fileName                                                 // Make a text file especially .gitignore

// Setting up a remote means to set-up a connection between the local computer and Online

$ git remote add nickName "Link"
$ git remote
$ git push -u nickName master

/* .gitignore is made to get rid of the unnecessary files while commiting. Type names of files in .gitignore
    Changes are made and are committed through the GUI
    CMD + SHIFT + . to view hidden files (.gitignore)
    
    To share any repository: 
    It is of the format https:// github . com / gitHubUserName / repositoryName
    
// Branches - Copy of main repository
Step 1) Make a branch - Make changes (public) & save it.
Step 2) These changes are then used to create a Pull request.
Step 3) Open Pull request - Resolve conflicts (Delete these <<<<<<<, =======, >>>>>>>) if any - Merge/Close that request
Step 4) Delete branch if not required

// Watch - To get notifications for a particular project
// Star - Bookmark particular project
// Fork - Pull requests to a friends program
// Raw - View the raw code or the raw format of the file
// Blame - Check the author name who created change(s)
// History - Check the history of commits

// Issue Tab - Making a to-do list
You can drag and drop image files, write the code using speical '< * >'
Label(Can be U.D.) - Desciding the problem, choosing Assignee(Can be changed later), Opening/Closing Issues
GitHub Wiki - Bio, Instructions, Documentation or any useful info. Can add more pages.
Can add custom _Sidebars

//Organization
Make Teams to work on specific areas - Add people, give them rights accordingly
- Owner
- Read Access
- Write Access

/* This is made solely for the purpose of my own learning. I do not intend to earn anything from here. I founded it helpful to keep all the commands that I
learned from Bucky Robert's tutorials and the one's that I learned on my own in one plac. However, I have kept it in public so that anyone can go through and
may be save a copy if it helps him/her too. Thank you! */
